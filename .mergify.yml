queue_rules:
  - name: default
    merge_conditions: []

pull_request_rules:
  - name: F2 bare merge when gate green + label
    conditions:
      - check-success=gate
      - label=go-f2
    actions:
      merge:
        method: squash
