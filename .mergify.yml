queue_rules:
  - name: default
    merge_conditions: []

pull_request_rules:
  - name: queue on approval + label
    conditions:
      - "#approved-reviews-by>=1"
      - "label=mergify-go"
    actions:
      queue:
        name: default
